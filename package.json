{
  "name": "pocketpulse",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm --filter @pocketpulse/core test"
  },
  "pnpm": {
    "overrides": {
      "postcss": ">=8.5.18",
      "sharp": ">=0.35.0",
      "vite": "^6.4.3",
      "esbuild": "^0.25.0"
    }
  }
}
